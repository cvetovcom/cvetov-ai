/**
 * Orders Types
 * TypeScript definitions for order-related entities
 */

export enum DeliveryType {
  DELIVERY = 'delivery',
  PICKUP = 'pickup',
}

export enum PaymentMethod {
  CARD_ONLINE = 'card_online',
}